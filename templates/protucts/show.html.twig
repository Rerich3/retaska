{% extends 'base.html.twig' %}


{% form_theme form 'bootstrap_4_layout.html.twig' %}

{% block title %}Protucts{% endblock %}

{% block body %}
    <h1>Protucts</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ protuct.id }}</td>
            </tr>
            <tr>
                <th>Produkt</th>
                <td>{{ protuct.Produkt }}</td>
            </tr>
            <tr>
                <th>No</th>
                <td>{{ protuct.no }}</td>
            </tr>
            <tr>
                <th>Cena</th>
                <td>{{ protuct.Cena }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('protucts_index') }}">back to list</a>

    <a href="{{ path('protucts_edit', {'id': protuct.id}) }}">edit</a>

    {{ include('protucts/_delete_form.html.twig') }}
{% endblock %}
